import { SERVER_URL } from '../config/environments/environments';

export const API_URL = SERVER_URL;

export const API_TIMEOUT = 10000;

export const API_HEADERS = { 'Content-Type': 'application/json' };
